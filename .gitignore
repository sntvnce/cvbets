# secrets — never commit
.env

# python
__pycache__/
.venv/

# SQLite side files (the .db itself IS tracked — it is the cloud loop's store)
data/*.db-shm
data/*.db-wal

# local logs (Actions regenerates its own summary via daily.py output)
data/daily.log